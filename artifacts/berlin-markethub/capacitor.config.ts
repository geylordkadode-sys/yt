import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.berlinmarkethub.app',
  appName: 'Berlin MarketHUB',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;